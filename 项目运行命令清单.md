# STFT-CNN4-MAML 项目运行命令清单

## 1. 进入项目目录

```powershell
cd C:\Users\JieZhang\Desktop\new-ifmaml\IFMAML-master
```

## 2. 第一次完整运行流程

第一次跑，或者原始数据、STFT 参数、信号通道读取逻辑发生变化时，建议从数据整理开始完整执行。

```powershell
python run_stft_cnn4_baseline.py --prepare
```

```powershell
python run_stft_cnn4_baseline.py --preprocess --clean_png
```

```powershell
python run_stft_cnn4_baseline.py --train
```

```powershell
python run_stft_cnn4_baseline.py --test
```

## 3. 快速测试流程

用于确认代码、数据读取、模型训练、测试和可视化流程能否跑通。

```powershell
python run_stft_cnn4_baseline.py --train --test --quick
```

说明：

| 项目 | quick 模式 |
|---|---|
| 训练 epoch | 3 |
| meta batch size | 4 |
| 测试 episode 数 | 4 |
| 用途 | 只用于检查流程，不作为正式实验结果 |

## 4. 正式基准实验流程

如果 `pu_data_mat` 和 `pu_data_processed` 已经确认正确，可以直接训练和测试。

```powershell
python run_stft_cnn4_baseline.py --train
```

```powershell
python run_stft_cnn4_baseline.py --test
```

正式实验默认参数：

| 参数 | 当前值 |
|---|---:|
| 任务设置 | 5-way 1-shot |
| 每类 query 数 | 15 |
| 正式训练 epoch | 250 |
| meta batch size | 16 |
| 测试 episode 数 | 100 |
| 测试内循环步数 | 10 |

## 5. 分步骤运行命令

如果需要单独调试某一步，可以使用下面的分步骤命令。

### 5.1 整理原始 PU `.mat` 数据

```powershell
python prepare_pu_baseline_data.py --raw_root "C:\数据集\德国帕德博恩轴承数据集" --mat_root ".\pu_data_mat"
```

### 5.2 重新生成 STFT 图片

```powershell
python pu_preprocess.py --clean
```

### 5.3 训练 STFT-CNN4-MAML

```powershell
python maml_train_pic.py --train --model_path ".\model_save\STFT_CNN4_MAML"
```

### 5.4 测试并生成可视化

```powershell
python maml_train_pic.py --test --model_path ".\model_save\STFT_CNN4_MAML"
```

### 5.5 训练后立刻测试

```powershell
python maml_train_pic.py --train --test --model_path ".\model_save\STFT_CNN4_MAML"
```

## 6. 可视化输出文件

训练和测试完成后，结果默认保存到：

```text
.\results
```

| 输出文件 | 生成阶段 | 说明 |
|---|---|---|
| `STFT_CNN4_MAML_training_history.csv` | train | 每个 epoch 的 train/valid loss 和 accuracy |
| `STFT_CNN4_MAML_training_curves.png` | train | 训练 loss / accuracy 曲线 |
| `STFT_CNN4_MAML_test_predictions.csv` | test | 每个 query 样本的真实类别和预测类别 |
| `STFT_CNN4_MAML_confusion_matrix_counts.csv` | test | 32 类混淆矩阵计数表 |
| `STFT_CNN4_MAML_confusion_matrix.png` | test | 32 类归一化混淆矩阵图 |
| `STFT_CNN4_MAML_tsne.png` | test | meta-test query 特征的 t-SNE 可视化 |

注意：当前模型是 5-way 1-shot MAML。测试时每个 episode 只抽 5 类，但代码会把每个 episode 的局部预测标签映射回原始 32 个故障类别，再汇总生成 32 类混淆矩阵。

## 7. 常用检查命令

### 7.1 检查 Python 文件语法

```powershell
python -m py_compile config_pu.py pu_loader.py pu_preprocess.py pu_dataset.py l2l_shim.py maml_model.py maml_train_pic.py run_stft_cnn4_baseline.py
```

### 7.2 检查 STFT 图片数量

```powershell
(Get-ChildItem -Path .\pu_data_processed -Recurse -Filter *.png).Count
```

期望结果：

```text
5120
```

### 7.3 查看模型权重

```powershell
Get-ChildItem -Path .\model_save
```

正式训练后应至少包含：

```text
STFT_CNN4_MAML_best
```

### 7.4 查看可视化结果

```powershell
Get-ChildItem -Path .\results
```

## 8. 后续缝合实验建议命名

为了避免不同模型的权重和图片互相覆盖，建议每个方案使用不同的 `model_path`。

```powershell
python maml_train_pic.py --train --test --model_path ".\model_save\STFT_LKNet_Attn_MAML"
```

对应输出会自动使用模型名作为前缀，例如：

```text
.\results\STFT_LKNet_Attn_MAML_training_curves.png
.\results\STFT_LKNet_Attn_MAML_confusion_matrix.png
.\results\STFT_LKNet_Attn_MAML_tsne.png
```

## 9. 当前注意事项

| 项目 | 说明 |
|---|---|
| STFT 信号通道 | 已修正为读取 `vibration_1` 等真实振动通道 |
| 旧模型权重 | 通道修复前训练得到的权重不能作为正式基准结果 |
| 正式 baseline | 建议重新运行 `python run_stft_cnn4_baseline.py --train` 和 `--test` |
| t-SNE 点数 | 默认最多取 1000 个 query 特征点，配置项为 `tsne_max_points` |
| 可视化开关 | 配置项为 `save_visualizations`，默认 `True` |
