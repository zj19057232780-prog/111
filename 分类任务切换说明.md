# PU 分类任务切换说明

项目保留原有跨工况设置：

```text
N15_M01_F10 + N15_M07_F04 + N15_M07_F10
                    ->
              N09_M07_F10
```

原始 `pu_data_processed` 目录仍按 32 个轴承编号保存。训练时，数据加载器根据
`config_pu.py` 中的分类预设动态合并轴承编号，不需要重新生成 STFT 图像。

## 切换方法

在 `config_pu.py` 中只修改：

```python
'classification_preset': 'artificial_9state',
```

可选值如下。

### artificial_9state

人工损伤 9 状态类别池，每个 episode 从 9 类中随机抽取 5 类。

| 状态类别 | 轴承编号 |
|---|---|
| Healthy | K001 |
| OR_EDM_L1 | KA01 |
| OR_Engraver_L1 | KA05 |
| OR_Engraver_L2 | KA03 |
| OR_Drilling_L1 | KA07 |
| OR_Drilling_L2 | KA08 |
| IR_EDM_L1 | KI01 |
| IR_Engraver_L1 | KI03 |
| IR_Engraver_L2 | KI07 |

### full_6state

覆盖全部 32 个轴承的 6 状态类别池，每个 episode 从 6 类中随机抽取 5 类。

| 状态类别 | 含义 |
|---|---|
| Healthy | 健康轴承 |
| Artificial_OR | 人工外圈损伤 |
| Real_OR | 真实外圈损伤 |
| Artificial_IR | 人工内圈损伤 |
| Real_IR | 真实内圈损伤 |
| Compound | 复合损伤 |

切换示例：

```python
'classification_preset': 'full_6state',
```

## 输出文件

默认模型名会自动追加预设名，例如：

```text
STFT_LSKLite_GFNetLite_ARSM_MAML_artificial_9state
STFT_LSKLite_GFNetLite_ARSM_MAML_full_6state
```

这样两个实验的模型、训练曲线、混淆矩阵和预测明细不会互相覆盖。

## 运行

配置切换后，直接重新训练：

```powershell
conda activate ifmaml2
cd C:\Users\JieZhang\Desktop\new-ifmaml\IFMAML-master
python maml_train_pic.py
```
